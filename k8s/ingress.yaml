apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appointment-app-ingress
spec:
  rules:
  - host: appointment-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: appointment-app-service
            port:
              number: 80
